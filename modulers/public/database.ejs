<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
  <title>Hello World</title>

  <!-- Bootstrap -->
  <link href="css/bootstrap.min.css" rel="stylesheet">

  <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>

<body>

  <nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        <a class="navbar-brand" href="/hello">Projet de test nodejs</a>
        <a class="navbar-brand" href="/database">Database</a>
        <a class="navbar-brand" href="/config">Configuration</a>
      </div>

    </div>
  </nav>

  <br><br><br><br>

  <div class="container">

    <div class="starter-template">
      <h1>MySQL database</h1>
    </div>

    <br>
    <%if (status == 'authenticated') { %>
      <div class="alert alert-success" role="alert">
        <h4 class="alert-heading">Connected <strong>successfully </strong> to mysql database !</h4>
      </div>
      <% } %>
        <%if (status != 'authenticated') { %>
          <div class="alert alert-danger" role="alert">
            <h4 class="alert-heading"><strong>Not connected</strong> to database </h4>
          </div>
          <% } %>
            <form action="/data">
              <div class="form-group">
                <label for="exampleFormControlTextarea1">MySQL Query</label>
                <textarea class="form-control" name="query" id="query" rows="3"><%= query %></textarea>
              </div>

              <button type="submit" class="btn btn-primary">Run query</button>  <a type="button" class="btn btn-warning pull-right" href="/reconnect">Reconnect</a>
            </form>
            <br>
                    <%if (time != undefined) { %>
            <p>Response time :<strong><%= time %></strong> ms</p>
                <% } %>
            <code><strong><%= result %></strong></code>
  </div>
  <!-- /.container -->


</body>

</html>
